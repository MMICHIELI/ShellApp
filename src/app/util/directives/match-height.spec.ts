/**
 * Created by mmichieli on 04/02/2018.
 */


/* let comp: TestComponent;

let fixture: ComponentFixture<TestComponent>;

// Test Component
@Component({
  template: `
  <div matchHeight="matchMe">
    <div class="matchMe" height="100"></div>
    <div class="matchMe" height="300"></div>
    <div height="500"></div>
    <div class="matchMe" height="200"></div>
  </div>`
})
class TestComponent {
}

beforeEach(() => {
  TestBed.configureTestingModule({
    declarations: [TestComponent, MatchHeightDirective],
  });
  fixture = TestBed.createComponent(TestComponent);
  fixture.detectChanges(); // initial binding

});

describe('Directive matchHeight=""', () => {

  let des = fixture.debugElement.query(By.directive(MatchHeightDirective));
// color tests
  it('should set all height to 300', () => {
    expect(true).toBe(true);
  });
})
*/
